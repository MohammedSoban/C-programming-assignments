#include <stdio.h>
#include <stdlib.h>

int main()
{
int a,b,area;
printf("enter the length:");
scanf("%d",&a);
printf("enter the width:");
scanf("%d",&b);
area=a*b;
printf("area of triangle:%d",area);

    return 0;
}
